[package]
name = "kronosdb-messaging"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tracing = "0.1"
parking_lot = "0.12"
dashmap = "6"
tokio = { version = "1", features = ["sync", "time"] }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
